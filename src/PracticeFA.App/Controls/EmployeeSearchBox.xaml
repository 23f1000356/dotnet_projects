<UserControl x:Class="PracticeFA.App.Controls.EmployeeSearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Border Style="{StaticResource CardBorderStyle}" Padding="12" Margin="0,0,0,12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Margin="0,0,12,0"
                       FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}"
                       Text="Badge"/>
            <TextBox Grid.Column="1" Padding="8,6" Margin="0,0,8,0"
                     Text="{Binding BadgeText, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="2" Content="Search" Click="Search_Click"/>
        </Grid>
    </Border>
</UserControl>
