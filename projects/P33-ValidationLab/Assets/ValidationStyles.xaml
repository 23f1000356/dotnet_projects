<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="ValidationErrorColor">#C62828</Color>
    <SolidColorBrush x:Key="ValidationErrorBrush" Color="{StaticResource ValidationErrorColor}"/>

    <!-- Red border + error message below field (P33 ErrorTemplate) -->
    <Style x:Key="ValidatedTextBoxStyle" TargetType="TextBox">
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
        <Setter Property="Validation.ErrorTemplate">
            <Setter.Value>
                <ControlTemplate>
                    <StackPanel>
                        <Border BorderBrush="{StaticResource ValidationErrorBrush}"
                                BorderThickness="2" CornerRadius="2">
                            <AdornedElementPlaceholder x:Name="Field"/>
                        </Border>
                        <TextBlock Margin="0,4,0,0"
                                   Foreground="{StaticResource ValidationErrorBrush}"
                                   FontSize="12"
                                   Text="{Binding ElementName=Field, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="ToolTip"
                        Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
